{
  "name": "take-one-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "./start.sh",
    "backend": ".venv-mac/bin/uvicorn server:app --reload --port 8000",
    "frontend": "cd frontend && npm run dev"
  }
}
