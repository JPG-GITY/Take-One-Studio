# Environment variables
.env
.env.local
.env.*.local

# Virtual environments
venv/
env/
ENV/
.venv/
.venv-mac/

# Do NOT ignore frontend source
!frontend/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Streamlit
.streamlit/

# Logs
*.log
logs/

# Cache
.cache/
.pytest_cache/

# Data
*.db
*.sqlite
.pinecone/

# Temporary files
tmp/
temp/
*.tmp

# OS specific
Thumbs.db
.DS_Store

# ── Take One Studio generated project data (app output — regenerated by running, NOT source) ──
# Heavy generated media in any project folder (covers future projects too)
**/Versions/*.png
**/Keyframes/
**/Storyboard/
**/Shots/
**/Exports/
**/Edits/
*.mp4
*.meta.json
# Sample / test project folders created by running the pipeline
/ConsistencyTest/
/F-AI-L/
/Nebula/
/NEBULA PROTOCOL Ep 3/
/VFX Shot/
/VFX Shot-v2/
/Shots/
/RECORDINGS/
/test-results/
# Stray secret note — the key is ALSO in git history (c4d9905); ROTATE it
Claude API KEY.txt

# Generated/live data + closed-door material (never commit)
/_studio/
/UNTITLED/
/AI-Hybrid Agentic Film - TV Pipelines/
*.pdf

# Generated project state — must never land in the worktree (see storage.py)
pipeline_state.json

# Credential backups — .env is ignored but its timestamped copies were not,
# and one of them (9 key lines) was one `git add -A` away from being committed.
.env.bak*
.env.*.bak

# Local-only tooling and test output
frontend/test-results/
.claude/settings.local.json
